<Window x:Class="BBC.BSC.Tool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BBC.BSC.Tool"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        
        Title="ALPHA 2019 BSC Tool" Height="550" Width="800" Topmost="False" Closing="Window_Closing" Icon="bsctoolicon.ico"
        >

    <Window.Resources>
        <CornerRadius x:Key="cornerRadius">8</CornerRadius>
    </Window.Resources>


    <!--<Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="#FFCDF3CD" Offset="0"/>
            <GradientStop Color="#FFCFC8F0" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>-->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Rectangle Fill="#FFF4F4F5" HorizontalAlignment="Stretch"  Grid.Row="2" Stroke="{DynamicResource PrimaryHueDarkBrush}" VerticalAlignment="Stretch"  Grid.ColumnSpan="2" Margin="0"/>

        <Expander Grid.ColumnSpan="2" Header="Click for help" BorderBrush="{DynamicResource PrimaryHueDarkBrush}" BorderThickness="1">
            <TextBlock x:Name="textBlock" HorizontalAlignment="Stretch" Margin="10,5" TextWrapping="Wrap" VerticalAlignment="Stretch"  >
                <Run Text="To start type a computer name in the search box and the results will be shown below."/><LineBreak/>
                <Run Text="You can search just the asset name: e.g. N334123 or the full host name."/><LineBreak/>
                <Run Text="Both AD and the CAT are searched."/><LineBreak/>
                <Run Text="- Ensure your ERE account is set in the bottom box."/><LineBreak/>
                <Run Text="- Select a host and the name will be copied to the connection box at the bottom."/><LineBreak/>
                <Run Text="- (You can manually edit the connection box)"/><LineBreak/>
                <Run Text="- Valid connection methods will be enabled."/></TextBlock>

        </Expander>


        <TabControl x:Name="tabControl" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  Grid.ColumnSpan="2" Grid.Row="1"  
                    Style="{StaticResource MaterialDesignTabControl}" 
                    BorderBrush="{DynamicResource PrimaryHueDarkBrush}" BorderThickness="1"
                    >
            <TabItem Header="SEARCH">
                <Grid Background="#FFE5E5E5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Rectangle x:Name="status" Fill="AliceBlue" HorizontalAlignment="Center" Margin="10" Height="24" Grid.Row="2" Stroke="Black" VerticalAlignment="Top" Width="25"/>
                    <TextBox x:Name="searchIn" HorizontalAlignment="Stretch"  Margin="10" TextWrapping="NoWrap" Text="" VerticalAlignment="Top" TextChanged="Text_Changed" Grid.Row="0" Grid.Column="1" />
                    <Label x:Name="label3" Content="Search term:" HorizontalAlignment="Right" Margin="8" VerticalAlignment="Center" />
                    <Label x:Name="label4" Content="Search results:" HorizontalAlignment="Right" Margin="8" Grid.Row="1" VerticalAlignment="Top"/>

                    <ListView x:Name="searchResults" 
                              HorizontalAlignment="Stretch" 
                              Margin="10" 
                              VerticalAlignment="Stretch" 
                              Grid.Row="1" Grid.RowSpan="2" Grid.Column="1" 
                              SelectionChanged="SearchResults_SelectionChanged" 
                              GotFocus="SearchResults_GotFocus" AlternationCount="1">
                        <ListView.View>
                            <GridView>

                                <GridViewColumn Header="Hostname" DisplayMemberBinding="{Binding Hostname}"/>
                                <!--<GridViewColumn Header="IP" DisplayMemberBinding="{Binding ip}" Width="100"/>-->
                                <GridViewColumn Header="Load Hostname" >
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Viewbox Height="25">
                                                <Button Content="{Binding Hostname}"  Click="Load_Result_Button_Click" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                            </Viewbox>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Load IP" >
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Viewbox Height="25">
                                                <Button Content="{Binding Ip}" Tag="{Binding Hostname}"  Click="Load_Result_Button_Click" />
                                            </Viewbox>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Description" DisplayMemberBinding="{Binding Description}"/>


                            </GridView>

                        </ListView.View>

                    </ListView>


                </Grid>
            </TabItem>
            <TabItem Header="History">
                <Grid Background="#FFE5E5E5">

                    <ListView x:Name="lvHisotry" SelectionChanged="LvHisotry_SelectionChanged" AlternationCount="1">


                    </ListView>
                </Grid>
            </TabItem>
        </TabControl>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Label x:Name="label1" Content="Connect to:"  Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Center"  FontWeight="Bold" />
            <Label x:Name="label2" Content="ERE account:" Grid.Row="1"  HorizontalAlignment="Right" VerticalAlignment="Center" />

            <TextBox x:Name="textbox_host"  Grid.Row="0" Grid.Column="1" TextWrapping="Wrap" Text="" VerticalAlignment="Center" Width="200" TextChanged="Textbox_host_TextChanged" FontWeight="Bold"/>
            <TextBox x:Name="textBox_ere"   Grid.Row="1" Grid.Column="1" TextWrapping="Wrap" Text="{local:SettingBinding ere}" VerticalAlignment="Center" TextChanged="TextBox_ere_TextChanged" />

        </Grid>
        <WrapPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Row="2" Grid.Column="1" Margin="10,0" >
            <Button x:Name="button_RDP" Content="RDP" Click="Connect_Button_Click" Margin="4" IsEnabled="False"  materialDesign:ButtonAssist.CornerRadius="{StaticResource cornerRadius}"/>
            <Button x:Name="button_RC" Content="RC"  Click="Connect_Button_Click" IsEnabled="False" Margin="4"  materialDesign:ButtonAssist.CornerRadius="{StaticResource cornerRadius}"/>
            <Button x:Name="button_VNC" Content="VNC" Click="Connect_Button_Click"  IsEnabled="False" Margin="4"  materialDesign:ButtonAssist.CornerRadius="{StaticResource cornerRadius}"/>
            <Button x:Name="button_SSH" Content="SSH"  Click="Connect_Button_Click"  IsEnabled="False" Margin="4"  materialDesign:ButtonAssist.CornerRadius="{StaticResource cornerRadius}"/>
            <Button x:Name="button_SSH_ERE" Content="SSH ERE"  Click="Connect_Button_Click"  IsEnabled="False" Margin="4"  materialDesign:ButtonAssist.CornerRadius="{StaticResource cornerRadius}"/>
            <Button x:Name="button_TELNET" Content="TELNET"  Click="Connect_Button_Click"  IsEnabled="False" Margin="4"  materialDesign:ButtonAssist.CornerRadius="{StaticResource cornerRadius}"/>
            <Button x:Name="button_HTTP" Content="HTTP"  Click="Connect_Button_Click"  IsEnabled="False" Margin="4"  materialDesign:ButtonAssist.CornerRadius="{StaticResource cornerRadius}"/>
            <Button x:Name="button_HTTPS" Content="HTTPS" Click="Connect_Button_Click" IsEnabled="False" Margin="4"  materialDesign:ButtonAssist.CornerRadius="{StaticResource cornerRadius}"/>

        </WrapPanel>




    </Grid>
</Window>
