<UserControl x:Class="BBC.BSC.Tool.GUI.Settings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BBC.BSC.Tool"
                  
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid Margin="10">
            <StackPanel>
                <TextBlock Style="{StaticResource MaterialDesignHeadline6TextBlock}">User settings</TextBlock>
                <TextBlock Style="{StaticResource MaterialDesignSubtitle1TextBlock}">Settings are saved automatically</TextBlock>
                <Separator Height="10" Margin="0"/>
                <StackPanel Orientation="Horizontal" Margin="10">
                    <Label Style="{StaticResource MaterialDesignLabel}">Eng Account:</Label>
                    <TextBox Width="250" Text="{local:SettingBinding ere}" VerticalAlignment="Center" TextChanged="TextBox_TextChanged" />
                </StackPanel>

            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
